<%= link_to gravatar_for(current_user, size: Settings.posts.size_img),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_my_profile"), current_user %></span>
<span><%= pluralize current_user.posts.count, t(".post") %></span>
